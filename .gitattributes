# Normalise all text files to LF in the repo (consistent Windows dev + Linux CI).
* text=auto eol=lf

# Binary assets — never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.pkl binary
*.joblib binary
